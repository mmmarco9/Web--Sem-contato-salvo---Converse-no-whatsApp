import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#5EBC67",
    },
    secondary: {
      main: "#464646"
    },
    action: {
      focus: "#5EBC67",
      selected: "#5EBC67",
      active: "#096A2E",
      hover: "#5EBC67",
    },
  },
});